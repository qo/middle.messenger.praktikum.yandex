export default `
div.form_window
    p(component="formTitle").form_window__title
    div.form_window__inputs
    div.form_window__buttons
        div(component="confirmButton").form_window__buttons__confirm
        div(component="cancelButton").form_window__buttons__cancel
`;